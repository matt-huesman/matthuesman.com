version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: [python3 portfolio/manage.py runserver]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
